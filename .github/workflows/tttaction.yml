name: learn-github-actions
run-name: ${{ github.actor }} is learning GitHub Actions
on: [push]
jobs:
  check-bats-version:
    runs-on: ubuntu-latest
    environment: ttt-env
    steps:
      - run: echo 123
      - run: touch ttt
      - run: echo ${{secrets.PPZ_NAME}} > ./ttt
      - run: cat ./ttt
